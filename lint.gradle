subprojects {
    apply plugin: 'java'
    apply plugin: 'io.spring.javaformat'
    apply plugin: 'checkstyle'

    dependencies {
        checkstyle 'io.spring.javaformat:spring-javaformat-gradle-plugin:0.0.46'
    }

    checkstyle {
        toolVersion = "10.12.4"
    }
}
